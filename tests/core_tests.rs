use anyhow::Result;

use cli_learning_tool::init;

//#[test]
//fn custom_bash_launches() -> Result<()> {
//    // spawns then kills the bash process using a builtin command to exit the session
//    // basis for most tests inside bash will require this and also internal checks, sending
//    // info to another tmp file with needed info
//
//    Ok(())
//}
//
//#[test]
//fn data_fetched_successful() -> Result<()> {
//    Ok(())
//}
